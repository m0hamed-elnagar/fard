import 'package:flutter/material.dart';

/// Define the new Twilight theme colors
class TwilightThemeColors {
  static const Color background = Color(0xFF0F111A);
  static const Color surface = Color(0xFF1B1E2B);
  static const Color primary = Color(0xFF8E9AAF);
  static const Color accent = Color(0xFFD4AF37);
  static const Color text = Color(0xFFECEFF4);
  static const Color textSecondary = Color(0xFFB0B8C6); // Slightly darker for secondary
  static const Color error = Color(0xFFF85149);
}
